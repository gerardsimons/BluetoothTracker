adb shell

am broadcast -a com.simons.bluetoothtracker.intent.action.rssi --ei rssi -10

am broadcast -a com.simons.bluetoothtracker.intent.action.rssi --ei azimuth_delta 20

for i in {1..18}
do
   am broadcast -a com.simons.bluetoothtracker.intent.action.rssi --ei azimuth_delta 20
   echo "Broadcasted"
done

for (( ; ; ))
do
   am broadcast -a com.simons.bluetoothtracker.intent.action.rssi --ei azimuth_delta 20
   sleep 0.5
   echo "infinite loops [ hit CTRL+C to stop]"
done